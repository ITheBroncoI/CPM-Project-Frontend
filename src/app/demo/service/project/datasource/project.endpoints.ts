import { baseUrl } from "../../baseurl";

export class ProjectEndpoints {
    static readonly nuevoProyecto = `${baseUrl}/proyecto`
    static readonly buscarProyectos = `${baseUrl}/proyecto/`
    static readonly buscarProyectoPorId = `${baseUrl}/proyecto/`
}
